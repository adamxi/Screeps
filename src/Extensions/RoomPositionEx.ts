import {PathHelper} from "./../Util/PathHelper";

export module RoomPositionEx {

    //RoomPosition.prototype.findCachedPathTo = function(x: number, y: number, opts ?: FindPathOpts): PathStep[];
    //RoomPosition.prototype.findCachedPathTo = function (target: RoomPosition | { pos: RoomPosition; }, opts?: FindPathOpts): PathStep[] {
    //    //RoomPosition.prototype.findCachedPathTo = function (a: any, b: any, opts?: FindPathOpts): PathStep[] {
    //    //(this as RoomPosition).findCachedPathTo(
    //    return null;
    //}

    //RoomPosition.prototype.findCachedPathTo = function (x: number, y: number, opts?: FindPathOpts): PathStep[] {
    //    return PathHelper.getPath(this as RoomPosition, new RoomPosition(x, y, (this as RoomPosition).roomName), opts);
    //}

    //RoomPosition.prototype.findCachedPathTo = function (target: RoomPosition | {pos: RoomPosition}, opts?: FindPathOpts): PathStep[] {

    //    return null;
    //    //return PathHelper.getPath(this as RoomPosition, new RoomPosition(x, y, (this as RoomPosition).roomName), opts);
    //}
}